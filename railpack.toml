[deploy]
startCommand = "python manage.py collectstatic --noinput && python manage.py migrate --noinput && python seed.py && gunicorn config.wsgi:application --bind 0.0.0.0:${PORT:-8080} --workers 2"
